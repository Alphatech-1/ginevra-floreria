<section
  id="inicio"
  class="relative overflow-hidden px-4 sm:px-6 lg:px-8 py-16 md:py-24 lg:py-32"
>
  <!-- Elementos decorativos de fondo -->
  <div class="absolute inset-0 z-0">
    <div class="absolute top-0 left-0 w-72 h-72 bg-primary/5 rounded-full blur-3xl -translate-x-1/2 -translate-y-1/2 animate-float-slow"></div>
    <div class="absolute bottom-0 right-0 w-96 h-96 bg-secondary/5 rounded-full blur-3xl translate-x-1/3 translate-y-1/3 animate-float-medium"></div>
    
    <!-- Patrón floral sutil -->
    <div class="absolute top-1/4 right-1/4 opacity-5">
      <svg class="w-48 h-48 text-primary" fill="currentColor" viewBox="0 0 24 24">
        <path d="M12,2L15.09,8.26L22,9.27L17,14.14L18.18,21.02L12,17.77L5.82,21.02L7,14.14L2,9.27L8.91,8.26L12,2Z" />
      </svg>
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      
      <!-- Texto con animaciones -->
      <div class="animate-fade-in-up">
        <div class="inline-flex items-center gap-2 mb-6 animate-slide-in-left">
          <span class="w-8 h-0.5 bg-primary"></span>
          <span class="text-primary font-medium tracking-wider">FLORISTERÍA ARTESANAL</span>
        </div>

        <h1 class="font-serif text-4xl sm:text-5xl lg:text-6xl xl:text-7xl text-primary leading-tight mb-6 lg:mb-8 animate-fade-in-up" style="animation-delay: 0.2s">
          <span class="block overflow-hidden">
            <span class="inline-block animate-slide-up" style="animation-delay: 0.3s">
              Flores que
            </span>
          </span>
          <span class="block overflow-hidden">
            <span class="inline-block animate-slide-up" style="animation-delay: 0.4s">
              transmiten
            </span>
          </span>
          <span class="block overflow-hidden text-transparent bg-clip-text bg-gradient-to-r from-primary to-secondary animate-slide-up" style="animation-delay: 0.5s">
            emociones reales
          </span>
        </h1>

        <p class="text-secondary/90 text-lg lg:text-xl mb-8 lg:mb-10 max-w-lg leading-relaxed animate-fade-in-up" style="animation-delay: 0.6s">
          En <span class="font-semibold text-primary">Ginevra</span> creamos arreglos florales únicos que capturan la esencia de tus momentos más especiales. Cada flor cuenta una historia.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 animate-fade-in-up" style="animation-delay: 0.7s">
          <a
            href="#contacto"
            class="group relative inline-flex items-center justify-center bg-gradient-to-r from-primary to-primary/80 text-white px-8 py-4 lg:px-10 lg:py-5 rounded-full text-lg font-medium hover:shadow-xl hover:shadow-primary/25 transition-all duration-500 hover:scale-105 overflow-hidden"
          >
            <span class="relative z-10">Contáctanos</span>
            <div class="absolute inset-0 bg-gradient-to-r from-secondary to-primary opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            <div class="absolute -inset-1 bg-gradient-to-r from-primary to-secondary rounded-full blur opacity-0 group-hover:opacity-50 transition-opacity duration-500"></div>
            <span class="absolute right-6 group-hover:translate-x-2 transition-transform duration-300"></span>
          </a>

          <a
            href="#nosotros"
            class="group inline-flex items-center justify-center border-2 border-primary/20 text-primary px-8 py-4 lg:px-10 lg:py-5 rounded-full text-lg font-medium hover:border-primary hover:bg-primary/5 transition-all duration-300"
          >
            Conoce más
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </a>
        </div>

        <!-- Stats o características -->
        <div class="flex flex-wrap gap-6 lg:gap-10 mt-12 lg:mt-16 animate-fade-in-up" style="animation-delay: 0.8s">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
              <svg class="w-5 h-5 text-primary" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
            </div>
            <div>
              <div class="font-bold text-xl text-primary">100%</div>
              <div class="text-secondary/70 text-sm">Frescas</div>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
              <svg class="w-5 h-5 text-primary" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
            </div>
            <div>
              <div class="font-bold text-xl text-primary">24h</div>
              <div class="text-secondary/70 text-sm">Entrega lista</div>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
              <svg class="w-5 h-5 text-primary" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd"/>
              </svg>
            </div>
            <div>
              <div class="font-bold text-xl text-primary">10+</div>
              <div class="text-secondary/70 text-sm">Diseños</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Imagen con efectos -->
      <div class="relative animate-fade-in-up" style="animation-delay: 0.4s">
        <div class="relative">
          <!-- Marco decorativo -->
          <div class="absolute -inset-4 lg:-inset-6 bg-gradient-to-br from-primary/10 to-secondary/10 rounded-3xl transform rotate-3 animate-rotate-slow"></div>
          
          <!-- Contenedor principal de imagen -->
          <div class="relative rounded-2xl lg:rounded-3xl overflow-hidden shadow-2xl hover:shadow-3xl transition-shadow duration-500 group">
            <img
              src="/hero-flores.jpg"
              alt="Arreglo floral elegante de Ginevra"
              class="w-full h-auto object-cover transform group-hover:scale-105 transition-transform duration-700"
              loading="eager"
            />
            
            <!-- Overlay de gradiente -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/10 via-transparent to-transparent"></div>
            


          <!-- Elementos decorativos adicionales -->
          <div class="absolute -bottom-4 -left-4 w-24 h-24 rounded-2xl bg-primary/10 border-8 border-white transform -rotate-12 hidden lg:block"></div>
          <div class="absolute -top-4 -right-4 w-20 h-20 rounded-full bg-secondary/10 border-8 border-white transform rotate-12 hidden lg:block"></div>

          <!-- Cita o texto decorativo -->
          <div class="absolute -bottom-1 right-8 bg-white rounded-xl p-4 shadow-xl max-w-xs transform rotate-2 hover:rotate-0 transition-transform duration-300 hidden lg:block">
            <div class="flex items-start gap-3">
              <div class="text-primary">
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
                </svg>
              </div>
              <p class="text-sm text-secondary italic">
                "La belleza de las flores habla cuando las palabras no alcanzan"
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animaciones personalizadas */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(100%);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes floatSlow {
    0%, 100% {
      transform: translateY(0) translateX(0);
    }
    33% {
      transform: translateY(-10px) translateX(10px);
    }
    66% {
      transform: translateY(5px) translateX(-5px);
    }
  }

  @keyframes floatMedium {
    0%, 100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-15px) translateX(-5px);
    }
  }

  @keyframes rotateSlow {
    0%, 100% {
      transform: rotate(3deg);
    }
    50% {
      transform: rotate(5deg);
    }
  }

  /* Clases de animación */
  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out forwards;
    opacity: 0;
  }

  .animate-slide-in-left {
    animation: slideInLeft 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-slide-up {
    animation: slideUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: floatSlow 8s ease-in-out infinite;
  }

  .animate-float-medium {
    animation: floatMedium 6s ease-in-out infinite;
  }

  .animate-rotate-slow {
    animation: rotateSlow 10s ease-in-out infinite alternate;
  }

  /* Mejora para la transición de imágenes */
  img {
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
  }
</style>