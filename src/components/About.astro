<section
  id="nosotros"
  class="relative overflow-hidden px-4 sm:px-6 lg:px-8 py-16 md:py-24 lg:py-32 bg-gradient-to-br from-white to-primary/3"
>
  <!-- Elementos decorativos de fondo -->
  <div class="absolute inset-0 z-0 overflow-hidden">
    <div class="absolute -top-24 -right-24 w-96 h-96 bg-primary/5 rounded-full blur-3xl animate-pulse-slow"></div>
    <div class="absolute -bottom-32 -left-32 w-96 h-96 bg-secondary/5 rounded-full blur-3xl animate-float-slow"></div>
    
    <!-- Patrones florales decorativos -->
    <div class="absolute top-1/3 left-1/4 opacity-[0.02]">
      <svg class="w-40 h-40 text-primary" fill="currentColor" viewBox="0 0 24 24">
        <path d="M12,2L15.09,8.26L22,9.27L17,14.14L18.18,21.02L12,17.77L5.82,21.02L7,14.14L2,9.27L8.91,8.26L12,2Z"/>
      </svg>
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      
      <!-- Imágenes con efectos -->
      <div class="order-2 lg:order-1 animate-fade-in-left">
        <div class="relative">
          <!-- Contenedor principal de imágenes -->
          <div class="relative">
            <!-- Imagen principal (grande) -->
            <div class="relative rounded-3xl lg:rounded-4xl overflow-hidden shadow-2xl group">
              <div class="aspect-square overflow-hidden">
                <img
                  src="/galeria-1.webp"
                  alt="Flores premium seleccionadas Ginevra"
                  class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-1000"
                  loading="lazy"
                />
              </div>
              <div class="absolute inset-0 bg-gradient-to-t from-black/20 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
              
              <!-- Badge de calidad -->
              <div class="absolute top-6 left-6 bg-white/95 backdrop-blur-sm rounded-full px-4 py-2 shadow-lg animate-float">
                <div class="flex items-center gap-2">
                  <svg class="w-4 h-4 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  <span class="text-sm font-medium text-primary">Premium Selection</span>
                </div>
              </div>
            </div>

            <!-- Imagen secundaria (superpuesta) -->
            <div class="absolute -bottom-8 -right-8 lg:-bottom-12 lg:-right-12 w-2/3 sm:w-1/2 lg:w-3/5 rounded-3xl overflow-hidden shadow-2xl border-4 border-white group-2 animate-fade-in-up" style="animation-delay: 0.3s">
              <div class="aspect-video overflow-hidden">
                <img
                  src="/galeria-4.webp"
                  alt="Arreglo floral artesanal Ginevra"
                  class="w-full h-full object-cover transform group-2-hover:scale-110 transition-transform duration-1000"
                  loading="lazy"
                />
              </div>
              <div class="absolute inset-0 bg-gradient-to-tr from-primary/20 to-transparent opacity-0 group-2-hover:opacity-100 transition-opacity duration-500"></div>
              
              <!-- Indicador de proceso artesanal -->
              <div class="absolute bottom-4 left-1/2 transform -translate-x-1/2 bg-white/90 backdrop-blur-sm rounded-lg px-3 py-1 shadow-lg">
                <span class="text-xs font-semibold text-primary flex items-center gap-1">
                  <svg class="w-3 h-3 animate-spin-slow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                  </svg>
                  Proceso Artesanal
                </span>
              </div>
            </div>

            <!-- Elemento decorativo abstracto -->
            <div class="absolute -top-6 -left-6 hidden lg:block">
              <div class="w-32 h-32 rounded-full border-8 border-white bg-primary/10 transform -rotate-12 animate-rotate-slow"></div>
            </div>
          </div>

          <!-- Estadísticas flotantes -->
          <div class="absolute -bottom-6 left-6 lg:-bottom-12 lg:left-20 bg-white rounded-2xl p-4 shadow-2xl hidden lg:block animate-fade-in-up" style="animation-delay: 0.6s">
            <div class="text-center">
              <div class="text-3xl font-bold text-primary mb-1">50+</div>
              <div class="text-sm text-secondary font-medium">Recuerdos plasmados</div>
              <div class="flex justify-center mt-2">
                <div class="flex space-x-1">
                  <div class="w-1 h-1 rounded-full bg-primary"></div>
                  <div class="w-1 h-1 rounded-full bg-primary"></div>
                  <div class="w-1 h-1 rounded-full bg-primary"></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Texto con animaciones -->
      <div class="order-1 lg:order-2 animate-fade-in-right">
        <!-- Badge de sección -->
        <div class="inline-flex items-center gap-2 mb-6 animate-slide-in-right">
          <span class="w-8 h-0.5 bg-gradient-to-r from-primary to-secondary"></span>
          <span class="text-primary font-medium tracking-wider text-sm uppercase">Nuestra Esencia</span>
          <span class="w-8 h-0.5 bg-gradient-to-l from-primary to-secondary"></span>
        </div>

        <!-- Título con gradiente animado -->
        <h2 class="font-serif text-4xl sm:text-5xl lg:text-6xl text-primary mb-6 lg:mb-8 leading-tight">
          <span class="block mb-2">Flores que</span>
          <span class="relative inline-block">
            <span class="relative z-10">permanecen</span>
            <span class="absolute bottom-0 left-0 w-full h-3 bg-primary/10 -rotate-1 animate-underline-expand"></span>
          </span>
          <span class="block mt-2">en la memoria</span>
        </h2>

        <!-- Contenido con animación escalonada -->
        <div class="space-y-6 mb-8 lg:mb-10">
          <p class="text-secondary/90 text-lg leading-relaxed animate-fade-in-up" style="animation-delay: 0.2s">
            En <span class="font-semibold text-primary">Ginevra</span> creemos que las flores no son solo un regalo, sino una experiencia significativa. Cada arreglo floral es una narración visual, diseñado con intención para evocar emociones genuinas y crear momentos inolvidables.
          </p>
          
          <p class="text-secondary/90 text-lg leading-relaxed animate-fade-in-up" style="animation-delay: 0.3s">
            Nos dedicamos a la búsqueda meticulosa de flores que destaquen por su frescura excepcional, formas únicas y carácter distintivo. Fusionamos el arte del diseño floral con una profunda sensibilidad estética para dar vida a piezas que trascienden lo convencional.
          </p>
        </div>

        <!-- Valores destacados -->
        <div class="grid sm:grid-cols-2 gap-6 mb-10 lg:mb-12">
          <div class="bg-white/50 backdrop-blur-sm rounded-2xl p-5 border border-primary/10 hover:border-primary/30 transition-all duration-300 group hover:shadow-lg animate-fade-in-up" style="animation-delay: 0.4s">
            <div class="flex items-start gap-4">
              <div class="flex-shrink-0 w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                </svg>
              </div>
              <div>
                <h4 class="font-semibold text-primary mb-2">Calidad Premium</h4>
                <p class="text-secondary/80 text-sm">Flores seleccionadas por su frescura y belleza excepcional</p>
              </div>
            </div>
          </div>
          
          <div class="bg-white/50 backdrop-blur-sm rounded-2xl p-5 border border-primary/10 hover:border-primary/30 transition-all duration-300 group hover:shadow-lg animate-fade-in-up" style="animation-delay: 0.5s">
            <div class="flex items-start gap-4">
              <div class="flex-shrink-0 w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 10h4.764a2 2 0 011.789 2.894l-3.5 7A2 2 0 0115.263 21h-4.017c-.163 0-.326-.02-.485-.06L7 20m7-10V5a2 2 0 00-2-2h-.095c-.5 0-.905.405-.905.905 0 .714-.211 1.412-.608 2.006L7 11v9m7-10h-2M7 20H5a2 2 0 01-2-2v-6a2 2 0 012-2h2.5"/>
                </svg>
              </div>
              <div>
                <h4 class="font-semibold text-primary mb-2">Diseño Único</h4>
                <p class="text-secondary/80 text-sm">Cada arreglo es una creación exclusiva y personalizada</p>
              </div>
            </div>
          </div>
        </div>

        <!-- CTA sutil -->
        <div class="animate-fade-in-up" style="animation-delay: 0.6s">
          <a 
            href="#contacto" 
            class="group inline-flex items-center text-primary font-medium hover:text-secondary transition-colors duration-300"
          >
            Descubre nuestra colección exclusiva
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-2 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animaciones personalizadas */
  @keyframes fadeInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fadeInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes pulseSlow {
    0%, 100% {
      opacity: 0.05;
    }
    50% {
      opacity: 0.1;
    }
  }

  @keyframes underlineExpand {
    from {
      width: 0;
      left: 50%;
    }
    to {
      width: 100%;
      left: 0;
    }
  }

  @keyframes spinSlow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  /* Clases de animación */
  .animate-fade-in-left {
    animation: fadeInLeft 1s ease-out forwards;
    opacity: 0;
  }

  .animate-fade-in-right {
    animation: fadeInRight 1s ease-out forwards;
    opacity: 0;
  }

  .animate-slide-in-right {
    animation: slideInRight 0.8s ease-out forwards;
    opacity: 0;
    animation-delay: 0.2s;
  }

  .animate-pulse-slow {
    animation: pulseSlow 4s ease-in-out infinite;
  }

  .animate-underline-expand {
    animation: underlineExpand 1s ease-out forwards;
    animation-delay: 0.5s;
  }

  .animate-spin-slow {
    animation: spinSlow 3s linear infinite;
  }

  /* Clases reutilizadas de secciones anteriores */
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: floatSlow 8s ease-in-out infinite;
  }

  .animate-rotate-slow {
    animation: rotateSlow 10s ease-in-out infinite alternate;
  }

  /* Hover effects personalizados */
  .group-2-hover\:scale-110:hover {
    transform: scale(1.1);
  }

  /* Efectos de transición */
  .transition-transform {
    transition-property: transform;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Mejoras de sombras */
  .shadow-2xl {
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  }

  /* Gradiente sutil de fondo */
  .bg-gradient-to-br {
    background-image: linear-gradient(to bottom right, var(--tw-gradient-stops));
  }
</style>